:root{
  --bg:#f4f7fb;
  --card:#fff;
  --muted:#6b7280;
  --accent:#0ea5e9;
  --gap:16px;
  --radius:10px;
  --shadow: 0 8px 22px rgba(15,23,42,0.06);
  --max-width:1000px;
}
*{box-sizing:border-box}
body{
  margin:0;
  font-family:Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
  background:var(--bg);
  color:#0f172a;
  line-height:1.45;
}
.container{
  max-width:var(--max-width);
  margin:0 auto;
  padding:20px;
}
.site-header{
  background:linear-gradient(90deg,#0f172a,#1e293b);
  color:#fff;
  padding:18px 0;
}
.header-grid{
  display:flex;
  justify-content:space-between;
  align-items:center;
}
.header-grid h1{margin:0; font-size:1.25rem}
.subtitle{margin:4px 0 0; color:rgba(255,255,255,0.85)}
.credit{opacity:0.9; font-size:0.9rem}

.card{
  background:var(--card);
  border-radius:var(--radius);
  padding:16px;
  box-shadow:var(--shadow);
  margin-bottom:var(--gap);
}

.search-row{
  display:flex;
  gap:8px;
  margin-top:8px;
}
input[type="search"]{
  flex:1;
  padding:10px 12px;
  border-radius:8px;
  border:1px solid #e6eef6;
  font-size:1rem;
}
button{
  padding:10px 14px;
  background:var(--accent);
  color:#fff;
  border:none;
  border-radius:8px;
  cursor:pointer;
  font-weight:600;
}
button:disabled{opacity:0.6; cursor:default}

.hint{color:var(--muted); margin-top:8px; font-size:0.95rem}

.result-section{margin-top:8px}

/* Current weather */
#current-weather{
  display:flex;
  gap:16px;
  align-items:center;
}
.weather-left{flex:1}
.weather-right{text-align:right; min-width:140px}
.city-name{font-size:1.15rem; font-weight:700}
.temp{font-size:2.4rem; font-weight:800; margin:6px 0}
.small{color:var(--muted); font-size:0.95rem}

/* Forecast grid */
.forecast-grid{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(120px,1fr));
  gap:12px;
  margin-top:12px;
}
.day-card{
  padding:12px;
  border-radius:8px;
  background:linear-gradient(180deg, rgba(14,165,233,0.05), rgba(255,255,255,0));
  text-align:center;
}
.day-card .date{font-weight:600; margin-bottom:6px}
.icon{font-size:1.6rem; margin-bottom:6px}

/* Suggestions */
.suggestions{margin-top:8px}
.suggestion{
  padding:8px;
  border-radius:8px;
  background:#f8fafc;
  margin-top:6px;
  cursor:pointer;
  border:1px solid #eef2f7;
}
.suggestion:hover{background:#eef8ff}

/* Error */
.error{background:#fff1f2; border:1px solid #fecaca; color:#b91c1c}

/* Utility */
.hidden{display:none}

.site-footer{padding:16px 0; text-align:center; color:var(--muted)}

@media (max-width:640px){
  #current-weather{flex-direction:column; align-items:flex-start}
  .weather-right{text-align:left}
}
